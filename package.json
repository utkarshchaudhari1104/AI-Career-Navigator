{
  "name": "ai-career",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:client": "vite --host 0.0.0.0",
    "dev:server": "python server/app.py",
    "start": "python server/app.py",
    "build": "vite build",
    "lint": "node --check public/app.js && node --check public/platform.js && node --check public/auth-email.js && python -c \"import ast; ast.parse(open('server/app.py', encoding='utf-8').read())\"",
    "preview": "vite preview"
  },
  "devDependencies": {
    "concurrently": "^10.0.5",
    "vite": "^8.3.0"
  }
}
